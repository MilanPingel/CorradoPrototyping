using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*

METHODS
wie kann man aus einer method() mehrere verschiedene werte rausholen --> strings, nummern, etc

CLASSES
1. Classes as behavioural components:
- These classes must inherit from Unity's Behaviour Classes


class : Enemy : MonoBehaviour {


int = restricted to script
static int = restricted to class
public static int = global variable


static class =
				1. all methods / variables inside must be static
				2. cannot be attached to objects

*/

/*

VECTORS






*/

public class NewScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
	}
}
